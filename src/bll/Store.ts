import { httpClient } from './../data-services/index';
import AuthenticationService from "../data-services/AuthenticationService";
import { authService } from '../data-services';

export class Store {
   
  constructor() {
    //   this.auth = new AuthenticationService(httpClient);
  }
}